services:
  - type: web
    name: mygpt-backend-node
    env: node
    plan: free
    branch: main
    rootDir: backend-node
    buildCommand: npm ci
    startCommand: npm start
    autoDeploy: false
    envVars:
      - key: NODE_ENV
        value: production
